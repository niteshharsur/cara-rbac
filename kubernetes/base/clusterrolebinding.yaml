apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRoleBinding
metadata:
  name: cara-rbac-collector-binding
subjects:
  - kind: ServiceAccount
    name: cara-rbac-collector
    namespace: cara-rbac
roleRef:
  kind: ClusterRole
  name: cara-rbac-collector
  apiGroup: rbac.authorization.k8s.io
